Attribute VB_Name = "modUtilText"
Option Explicit

' ============================================================================
' modUtilText - テキストファイル入出力と、複数モジュールに散っていた
'   「同じ算数」を1本にまとめるための共通部品置き場(基盤層)。
' ----------------------------------------------------------------------------
' 2026-07-31(R11-F1): R11-F2 の重複統合(UTF-8読み書き / 移動平均 /
'   ページ分割の添字計算 / Timer日跨ぎ)の受け皿として新設した。
'   modUtil は27,822字で余地が少ないため、新しい共通部品はこちらへ置く。
'
' 置くもの / 置かないもの:
'   ・置く: 2箇所以上で同じ答えを出す必要がある処理(憲章§4-5「同型の問題は
'     共通部品で一度だけ解決する」)。ADODB.Stream によるUTF-8の読み書き、
'     経過ミリ秒の算出、ページ分割の添字計算など。
'   ・置かない: 画面(Shape/Range)を触るもの、業務ロジックの判断。
'     基盤層なので上位層(ingest/qa/pack/stats/ui)を参照してはならない。
'
' 本コミット(R11-F1)では枠と役割だけを置き、実装は R11-F2 で入れる。
' ============================================================================
