# 次スレッドの始め方（コピペ用プロンプト集）

> このスレッドはコンテキストが一杯。続きは新しいスレッドで。
> 下のテンプレートを**そのままコピペ**して使う。`【】`の中だけ自分の状況に置き換える。
>
> 共通のコツ：**最初に「リポジトリと開発ブランチ」を伝え、関連docを読ませてから**本題に入る。
> そうすると私（Claude）が過去の文脈を正確に復元できて、ミスが減る。

- リポジトリ：`kojima1459/notebook`
- 開発ブランチ：`claude/internal-notebook-lm-chatbot-B6BE7`

---

## ① Phase 3/4 の実装 ＋ 精度検証の修正を反映したいとき

```
リポジトリ kojima1459/notebook、ブランチ claude/internal-notebook-lm-chatbot-B6BE7 で作業して。

まず docs/v2/00_概要と現在地.md、10_アーキテクチャと肝.md、50_ロードマップ.md を読んで
現状を把握して。

やってほしいこと：
1. 精度検証で出た修正を反映する。検証結果データを添付する（△・×の質問と正しい情報）。
   → これを feedback シード or knowledge_base の修正として取り込む方針を提案してから実装して。
2. その後 Phase 3b（チャット吹き出しUI）/ Phase 4（【SharePoint連携 等、やりたい範囲】）に着手。

制約は今まで通り：動かない・エラーは論外。社内PCでしかテストできない。
ステップバイステップで、出戻りないように。勝手に飛ばさず、要所で確認して。
```
※ 検証データは Excel/CSV/スクショどれでも添付OK。`docs/v2/50` の「合格基準」前提で。

---

## ② 精度の修正だけ（Phaseは進めない）したいとき

```
リポジトリ kojima1459/notebook、ブランチ claude/internal-notebook-lm-chatbot-B6BE7。
docs/v2/10 と 60 を読んで。

精度検証の結果を添付する。△・×だった質問と「正しい答え・参照条文」を渡すので、
- knowledge_base のチャンク追加/修正、または
- feedback の承認済みQ&Aとして取り込み
のどちらが適切か判断して反映して。ビルドまでやって、社内PCでの確認手順も教えて。
```

---

## ③ 団体障害グループ（横展開）版を作りたいとき

```
リポジトリ kojima1459/notebook、ブランチ claude/internal-notebook-lm-chatbot-B6BE7。
docs/v2/60_横展開_再現手順書.md を読んで、その手順どおりに団体障害グループ版を作って。

資料を添付する：【約款/ガイドライン/マニュアル/重説/商品部Q&A（PDFか貼付テキスト）】
部署：dept_id=group_disability、名称=団体障害グループ。

VBAコードは原則さわらず、ナレッジ入れ替えで。ビルドまでやって、
社内PCでのベクトル化手順も最後にまとめて。
```

---

## ④ リボンちゃんで新しいツールを作りたいとき

```
リポジトリ kojima1459/notebook を参考に、リボンちゃんで新ツールを作りたい。
docs/v2/80_リボンちゃん開発キット.md を読んで。

下のインテーク質問票を埋めたので、これを前提に設計して：
- やりたいこと：【…】
- 入力/出力：【…】
- 使うAI機能：【チャット/埋め込み/Vision/画像生成/音声/翻訳 等】
- 対象アプリ：【Excel/Word/PPT】
- 環境制約：社内PC・ターミナル不可・マクロ可否【…】
- データ：【…】 / 配布：【…】 / 成功基準：【…】

まず「リボンちゃんで実現可能か（80のB章/D章）」を判定してから、
本ボットの部品（自己インストーラ・窓口モジュール・config駆動）を流用して設計して。
```

---

## ⑤ ただ仕様を思い出したい/説明したいとき

```
リポジトリ kojima1459/notebook の docs/v2/ を読んで、
このチャットボットの【仕組み/肝/リボンちゃん仕様/横展開手順】を説明して。
```

---

## 添付データの渡し方（非エンジニア向けメモ）

- **精度検証結果**：`精度検証シート.xlsx` をそのまま添付でOK。○△×と改善メモが入っていれば私が読む。
- **約款等の資料**：PDF添付 or 本文を貼り付け（社内PCでPDFを出せない時は貼付でOK。実績あり）。
- **エラー**：スクショ全文＋「どのステップで止まったか」。
- **本番ファイル**：ベクトル化済み `Chatbot_v2.xlsm` は社内PCにしかない貴重品。共有フォルダ等に退避しておく。
