# ロードマップ・今後のタスク

> 現在地（2026年6月）と、これから。フェーズ番号は実装単位で振り直してある。

---

## 現在地（DONE）

| フェーズ | 内容 | 状態 |
|---|---|---|
| Phase 0+1 | 単一xlsm・自己インストール・パイプライン・mock動作 | ✅ 完了 |
| Phase 2 | ハイブリッド検索（埋め込み40件→LLM12件、`rag_enabled`ゲート） | ✅ 完了・**本番PC検証済** |
| Phase 3a | 評価モデル刷新 🟢🟡🔴 ＋ 教えてBOX誘導 | ✅ 完了・本番PCでボタン表示確認 |

**本番PCでの確認（2026-06-29）**：リボン接続OK（1672ms）／990件ベクトル化 成功（失敗0）／`rag_enabled=TRUE`。

---

## 次にやること（優先順）

### 最優先：パイロット運用 → 現場の声
- 同じ部署の数名に配布し、**1〜2週間** 実務で使ってもらう。
- 精度検証は「合格基準を**事前合意**」してから（重箱の隅で無限ループに入らないため）。
  - 推奨基準：○が75%以上 かつ × が0件。△は「ナレッジ追加で継続改善」扱いで合格条件に含めない。
  - 対象外：ナレッジ未登録の制度／意地悪質問／直近改定情報。
  - 位置づけ：**全知ツールではなく、日常照会を減らす補助ツール**として評価。
- ここで出た「本当に困っていること」を見てから、下の Phase を設計する。

### 横展開：団体障害グループ
- 資料提供を受けたら `60_横展開_再現手順書.md` の通りにナレッジ入れ替えで再現。
- コードは原則さわらない。

### Phase 2.5（任意・最適化）：次元削減
- 1536次元 → 256次元に切り詰めるとコサイン計算が約6倍速。
- text-embedding-3-small は MRL 訓練済みで、先頭を切っても精度劣化が小さい。
- **制約：再ベクトル化の前にしか入れられない**（保存済みCSVと整合させるため）。
- 本番が安定し、かつ体感速度が問題になったら着手。`emb_dims` config を足して実装。

### Phase 3b（任意・UX）：チャット吹き出しUI
- TextBox/Shape ベースの会話風UI。**機能は増えず見た目だけ**。
- 一番壊れやすい部分なので、実機で使われ始めて要望が出てから。

### Phase 4（未定義・要情シス調整）
- SharePoint連携（承認済みfeedbackの全社配信）／管理者用 承認UI。
- 利用ログ・コストの集計ダッシュボード。
- 部署マスタ拡充・全社横断ナレッジ整理。
- 全社横断の巨大統合になったら、ハイブリッド検索のリランキング強化が前提になる。

---

## モデル/コストの調整（いつでも・コード変更なし）

情シスから「コスト」と言われたら config を触るだけ：
1. `router_model` を `gpt-5.4-nano` に
2. `verifier_enabled` を `False` に（30%以上減）
3. さらに `verifier_model` を軽量モデルに、ルーター件数 12→ 少なめに

---

## 判断の原則（迷ったら）

- **完璧を待ってリリースしない。** 60〜80点で出して現場の声で埋める。
- **失敗しても元に戻る経路がある変更を優先する。** （`rag_enabled` のようなゲート）
- **コードよりデータ（シート/config）で挙動を変える。**
