# R36 是正の仕組み・スクショ本文表示・画像を📁から・置き場所（2026-09-05 起草）

**司令塔裁定書。実装波はこの文書だけを読めば着手できる状態を目標にする。**
判定基準は `00_プロダクト憲章.md`、現在地は `HANDOFF_再開手順.md` 冒頭。
前提の実装調査（2026-09-05・司令塔が実装で再現済み）は HANDOFF R35節「髙橋フィードバックの司令塔調査結果」に要約。

---

## 0. 起点

| 日付 | 出来事 |
|---|---|
| 09-04 | R35 本体（方式B）が実Excel で合格。追補 F3a（パック取込）/F3b（往復 bat）を実装。実機1往復は未確認 |
| 09-05 | 事務局・髙橋さんのテスト所感（要旨）: ①スクショ取込の読み取り結果を見たい・直したい ②自分のナレッジで誤答したとき次から直るようにしたい（是正・学習） ③取込/出力/削除を分かりやすく ④NotebookLM 並みの精度 ⑤「Excel のプロセスを分ける」のような工夫をもっと |
| 09-05 | ユーザー裁定: **R36 は「置き場所（データブック分離）＋是正の仕組み＋スクショ全文表示＋画像を📁から」に絞る**。精度は数字を取ってから別ラウンド |
| 09-05 | 説明書の認知負荷対策として `docs/05_図解ガイド.html` を追加済み（③の文書側は完了。UI 側は本ラウンドで最小限） |

**この4件を選んだ理由（髙橋さんの言葉を実装の現状に当てたもの）**

| 要望 | 実装の現状（再現済み） | 本ラウンドの答え |
|---|---|---|
| スクショの読み取り結果を見たい | 転写全文は `my_knowledge.full_text`（veryHidden）。見えるのは抜粋 150/300/600 字だけ。編集→保存し直しは無い | **§3 本文表示**（新画面 `text_view`）。編集は「消して ➕登録し直す」導線の案内に留める（§3-5） |
| 画像を簡単に放り込みたい | png/jpg は `📁 追加` の FileDialog で選べない（`SUPPORTED_EXTS` に無い。入口は 📸 のみ） | **§4 画像を📁から** |
| 誤答を次から直したい | ❌違う→「修正ナレッジ」が普通の資料として1冊増えるだけ。**質問文が本文に無い・優先されない・誤資料は残る**。トースト「次から…この内容で答えます」は過大表現 | **§2 是正の仕組み**（質問文を保存し、回答生成時に是正を最優先で注入） |
| 置き場所 | R35 F3b の往復 bat（xlsm ごと D: と OneDrive を往復）。実機1往復は未確認 | **§1 置き場所**（分岐あり。ユーザー選択待ち） |

---

## 1. 置き場所（データブック分離）— 分岐あり・GO 待ち

### 1-1. 事実（R35 §7・§10 と 09-05 実測）

- 会社PCのローカル（D:・Documents・Downloads）は日跨ぎ／シャットダウンで消える。残るのは OneDrive 同期フォルダ・部門共有サーバ・デスクトップのショートカットだけ。
- OneDrive 直接起動は避ける（`ThisWorkbook.Path` が URL になりうる。実測なし・設計で回避）。信頼できる場所の追加は組織ポリシーで不可。
- 環境変数 `%OneDrive%` は別利用者を指す（09-05 実測）→ 環境変数で置き場所を探す方式は不採用（姉妹PJも撤去中）。
- 現行（F3b）: bat が xlsm ごと `D:\MyBookshelf\` へ複製して起動し、10分ごと＋終了時に OneDrive 側へ書き戻す。データは xlsm の中。**版上げ（新 zip 配布）のたびに本棚が消える**（引き継ぎファイルを手で作る必要がある）。

### 1-2. 選択肢（タクシー原則）

| | 1-A **版上げ時の自動引き継ぎ**（推奨） | 1-B 本格的なデータブック分離 |
|---|---|---|
| 何をするか | データは xlsm の中のまま。**新しい版を初めて開いたとき、同じフォルダにある前の版（`MyBookshelf_前回.xlsm` など）を見つけて、本棚・実績・設定を自動で取り込む**（既存の `modMigrate.ImportUserData` の中身を、引き継ぎ .xlsx ではなく前版 .xlsm から直接読む形へ広げる） | エンジン（xlsm・D:）とデータ（.xlsx・OneDrive）を分け、起動時に読み込み・終了時に書き出す |
| メリット | 髙橋さんの困りごと（版上げで消える）が消える／bat・VBA の往復設計は不変／実装が小さい（modMigrate の読み取り元を広げる＋起動時の1判定） | 版上げが「xlsm を置き換えるだけ」になる／データが .xlsx なので AV・メールに通りやすい |
| デメリット・リスク | 前版の xlsm を `Workbooks.Open` するので **`Application.EnableEvents=False` で開く**（R35 以前の版は `Workbook_Open` に自己インストーラを持つ。走らせてはいけない）／前版が読み取り専用・破損なら F3a のゲートで読まずに止める | **重い**: `my_vectors` は 20,500 行×768 次元の CSV 文字列。全量の書き出し・読み込みを毎回やると起動と終了が数分になりうる（未計測）／凍結 modBoot に起動時の読み込みを足せない（Auto_Open→Boot の外に置く必要）／設計 2 ラウンド |
| おおよその時間 | 実装波 1（Sonnet）＋レビュー 2周 | 設計 1 ラウンド＋実装 2 波＋実機 2 往復 |

**司令塔の推奨は 1-A。** 1-B は「起動・終了の所要秒数」を実測してから（R37 候補）。1-A は 1-B の前提（前版からの読み取り）を先に作るので無駄にならない。
**この節だけはユーザーの GO を待って着手する。** §2〜§4 は先行して実装する。

### 1-3. 1-A の契約（GO 後に実装波へ渡す）

- 起点: `modTour.StartTourIfFirstRun` の直後（ウィザードと同じ「初回だけ」の場所。凍結 modBoot には触らない）。判定: 本棚が空（`my_manifest` 0件）**かつ** `ThisWorkbook.Path` に `MyBookshelf_前回.xlsm`（bat が退避する名前・`_launcher_bat_text` の `mark`/退避名と一致させる）がある。
- 問い: 「前の版の本棚が見つかりました（N件・保存日時）。この版へ引き継ぎますか?」（はい/いいえ）。いいえは二度と聞かない（`ui_state` に印）。
- 読み取り: `modMigrate.ImportUserData` と同じ列マッピングを **前版の `my_knowledge`/`my_vectors`/`my_manifest`/`my_stats`/config（`CFG_KEEP_KEYS` のみ）** から読む。開く前に `modShelfScan.EncryptedFileKind` → `modPack.OpenGateReason`（F3a と同じゲート）。`Application.EnableEvents = False`・`ReadOnly:=True`・`UpdateLinks:=0`。
- 版の互換: `embed_dim` 不一致なら取り込まない（E0701 と同じ文言型）。
- 容量: modMigrate 残 1,429 字 → **実体は新モジュール `modMigrateFrom`（受け皿）に置き、modMigrate からは1行呼び出し**。

---

## 2. 是正の仕組み（❌違う → 次から必ず使われる）

### 2-1. 現状（再現済み・`modAppAct.RecordCorrection:115-143`）

1. ❌違う → `modAsk.FeedbackRed`（記録＋共有）→ InputBox「正しい内容を教えてください」
2. 本文 = `【修正ナレッジ】` + 「対象の回答(抜粋): 」回答先頭400字 + 「正しい内容: 」入力 → `modVault.RegisterKnowledgeText("修正ナレッジ", body, "修正,フィードバック")` → TEMP に `ナレッジ_修正ナレッジ_yyyymmddhhnnss.txt` → `modShelf.IngestFile` → **普通の資料**として `my_knowledge` へ
3. トースト「ありがとうございます。次に同じ質問をした人から、この内容で答えます。」

問題: **元の質問文が本文に無い**（同じ質問を再入力しても、回答抜粋に偶然含まれる語でしか当たらない）／優先度・加点が無い／誤っていた元チャンクは残る／修正のたびに別資料が増える。

### 2-2. 裁定（2-A: 修正ナレッジの強化。別テーブル＋一覧画面の 2-B は採らない）

UI は増やさない。**「質問文を一緒に保存する」＋「回答生成の直前に、質問が一致する是正を必ず先頭に差し込む」** の2点で「次から必ず使われる」を実現する。

1. **保存する本文**（modAppAct）:
   ```
   【是正メモ】この質問には、下の「正しい内容」で答えること。
   質問: <元の質問文そのまま>
   正しい内容: <入力>
   （誤りだった回答の抜粋: <回答先頭200字>）
   ```
   - 資料名（title）は `是正メモ_<質問先頭24字>`（`SanitizeFileName` 相当の置換は modVault 側の既存処理に従う）。同じ質問への2回目は**上書き**（同名の既存資料があれば `modShelf.DeleteSource` してから登録。「修正のたびに増える」を止める）。
   - `keywords` 引数は `"是正,修正,フィードバック"`。origin は `self` のまま（パック・チャンネル・引き継ぎの既存経路を壊さない）。
2. **回答時の注入**（新モジュール `modCorrect`・qa 層・凍結外）:
   - 入口は `modAskRetrieve` の各 Run*（`RunMultiRetrieve` / `RunDeepScoped` / `RunUnscoped`）が hits を返す**直前**に1行 `modCorrect.InjectHits question, hits`。modAskRetrieve は残 1,092 字なので**1行呼び出し×3 に留める**。
   - `InjectHits` の中身: `my_knowledge` から資料名が `是正メモ_` で始まる行を集め、本文の `質問: ` 行を取り出し、`modSparse.NormalizeForSearch` で正規化した質問と**完全一致**、または（完全一致が無ければ）`modSparse.KeyScore` 系の語一致が閾値以上（config `correct_key_min`、既定は実装波が Pure テストで決めて報告）の是正メモを、**hits の先頭に**差し込む（既存 hits の同一 chunk は除く。件数上限は `topk_*` を超えないよう末尾を落とす）。
   - hit の形は modAskRetrieve が返しているものと同じ（調査結果 §2-4 の型に従う）。**modAsk・modPrompts には触らない**（差し込んだ hit は普通の出典として本文へ載り、チップにも出る。本文冒頭の「【是正メモ】この質問には…で答えること」が LLM への指示として働く）。
   - 一般アシスタント（`sendMode="normal"`）では注入しない（本棚を見ないモード）。
3. **トースト文言**を実態に合わせる: 「ありがとうございます。この質問には次から、いま書いていただいた内容で答えます（是正メモとして本棚に入りました）。」
4. **記録のみ（本ラウンドでやらない）**: 誤チャンクの減点・無効化／他人の `correction` 受信箱の取り込みボタン／是正メモの一覧画面（本棚の一覧で `是正メモ_` を検索すれば見える）。

### 2-3. 契約（実装波向け・調査結果反映後に確定する箇所は【調査】印）

- `modCorrect.InjectHits(ByVal question As String, ByRef hits As <hit型>【調査】) As Long`（差し込んだ件数を返す）。純関数部分 `MatchCorrection(ByVal qNorm As String, ByVal memoText As String) As Boolean` と `ExtractQuestionLine(ByVal memoText As String) As String` を分けて Pure テスト可能にする。
- `modAppAct.RecordCorrection`: 本文組み立てを `modCorrect.BuildMemoBody(question, fixText, answerExcerpt)` に移し、modAppAct からは1行（modAppAct は残 14,963 字だが、是正の知識を1モジュールに集める）。質問文は `modAppState` が持つ「対象の質問」【調査: `modAppState.TargetQuestion` 相当の有無】。
- 同名上書き: `modShelf.DeleteSource`（凍結 modShelf の**既存 public**を呼ぶだけ）→ 登録。
- 新 config: `correct_inject` = `on`（`off` で注入しない・既存挙動）／`correct_key_min`（語一致の閾値）。`build_config_rows` に説明つきで追加。

---

## 3. スクショ本文表示（読み取り結果をその場で確かめる）

### 3-1. 裁定

- 新画面 **`text_view`**（`modDiag` の `diag_report` シートと同じ作法: 実行時に作る作業シート、テキストを行に流し込み、左上「← 戻る」）。
- 入口は2つ:
  1. マイ本棚ツールバー（一覧表モードのみ）に **`📖 本文`** ボタン（`🗑 1件` と同じ「行をクリックしてから押す」型。kind は通常）。行未選択の文言も `🗑 1件` と同型: 「本文を見たい資料の行をクリックしてから、もう一度押してください。」
  2. **📸 スクショ取込の完了直後に自動で `text_view` を開く**（髙橋さんの「放り込んだあと問題ないか確認したい」に1手で応える）。config `screenshot_show_text` = `TRUE`（`FALSE` で従来どおりトーストのみ）。
- 表示内容: 資料名／取込日時／チャンク数／`【p.N】` 区切りで **全チャンクの `full_text` をページ順に**（breadcrumb 前置 `【資料名>章>条】` は除去して表示。`modVaultGallery.MakePreviewText` と同じ剥がし方）。末尾に案内: 「内容が違うときは、この資料を 🗑 で消してから ➕登録 で正しい文章を登録し直してください（読み取った文章を直接直す機能はありません）」。
- 戻る先: 開いた元の画面（マイ本棚 or チャット）。`modUiLock`・`modViewport` の作法は modDiag と同一にする【調査】。

### 3-2. 契約（【調査】反映後に確定）

- 新モジュール `modTextView`（ui 層・受け皿）: `ShowForSource(ByVal srcName As String, ByVal backTo As String)`／`OnBack()`／純関数 `StripBreadcrumb(ByVal s As String) As String`（Pure テスト）。
- ツールバー: `modKnowledgeBar.ToolbarSpec` に `📖 本文` を追加（table モードのみ）。`OnToolbarLegend` の説明表にも1行。modKnowledgeBar 残 5,999 字。
- 行→資料名の解決は `modUIShelf.OnDeleteSource` が使っている既存関数を再利用【調査】。modUIShelf は残 96 字なので**触らない**（📸 完了後の自動表示は `OnIngestScreenshot` 末尾に1行足す必要があるが 96 字では入らない → `RefreshBadgesAndDashboard` の呼び出しを `modTextView.AfterScreenshot destPath` に置き換え、その中から既存の再描画と本文表示を両方呼ぶ。**同じ字数以内**で差し替える）。

---

## 4. 画像を 📁 から入れる

### 4-1. 裁定

- `📁 追加`（ナレッジ画面・チャット画面の 📁 とも `modShelfBatch.AddFilesViaDialog`）の FileDialog フィルタに **`*.png;*.jpg;*.jpeg` を足す**（`modFeatures.FeatureEnabled("vision")` が真のときだけ。偽なら従来どおり）。
- `SUPPORTED_EXTS`（`modExtractor:51`）は**変えない**。画像は今までどおり `E0301`→`modShelfVision.TryVisionFallback` の経路で読む（`IsImageFileExt` が判定）。したがって `IngestFile` 側の変更は不要【調査: 途中に拡張子で弾く関所が無いか】。
- フォルダ同期（`modShelfScan.IsSupportedExtLocal`）は**変えない**（記録のみ: 📸 が保存する jpg が本棚フォルダに入るため、同期が画像を拾うと二重取込の縫い目になる）。
- 「使い方」シート ④ の対応形式表と `docs/05_図解ガイド.html` §5 を「画像（png/jpg）は 📁 追加からも」へ更新（build の `_make_howto` と HTML の2箇所）。

### 4-2. 契約

- `modShelfVision` に `Public Function ImageDialogPattern() As String`（`"*.png;*.jpg;*.jpeg"`。`IsImageFileExt` と同じ一覧から組む。純関数）。
- `modShelfBatch.BuildFilterPattern` の末尾で `If modFeatures.FeatureEnabled("vision") Then ... & ";" & modShelfVision.ImageDialogPattern()`（modShelfBatch 残 2,003 字）。
- Pure テスト: `ImageDialogPattern` の内容／`BuildFilterPattern` は FileDialog 依存なので対象外（記録）。

---

## 5. 波の構成（司令塔がオーケストレーション）

| 波 | 内容 | 担当 | 触るモジュール |
|---|---|---|---|
| 調査 | §2〜§4 の【調査】印を実装で確定（hit 型・行→資料名・diag_report 作法・IngestFile の関所） | Explore | 読むだけ |
| 波1 | §4 画像を📁から ＋ §3 本文表示 | Sonnet | modShelfVision / modShelfBatch / 新 modTextView / modKnowledgeBar / modUIShelf(同字数差し替え) / modTestsPure38 / modules.json / build howto |
| 波2 | §2 是正の仕組み | Opus | 新 modCorrect / modAppAct / modAskRetrieve(1行×3) / build config / modTestsPure38 |
| 波3 | 文書（使い方シート・05_図解ガイド・10_使い方ガイド・FEATURES・MASTER_SPEC の text_view 追記） | Haiku | docs / build `_make_howto` |
| 波4（GO 後） | §1-A 版上げ時の自動引き継ぎ | Sonnet | 新 modMigrateFrom / modMigrate(1行) / modTour / bat 名の照合 |
| レビュー | 敵対的レビュー1周目（壊す班／残骸班）→ Fix → 2周目（Fix 検証） | Opus×2 | — |

**検問方式**: 着手時に空コミット→項目毎に即コミット→司令塔が push。サブエージェントは発見したエラー・仕様との食い違いを勝手に直さず報告。

## 6. Definition of Done（R36）

1. `vba_lint` ERROR 0／`run_lo_tests --mode compile` OK→`--mode pure` 全PASS（PASS 下限は新テスト分だけ上げる。並列禁止）
2. `build --dev`／`--prod --zip` 自己検証 PASS／`bin_roundtrip` OK／`lo_xlsm` 157+新規本数 OK
3. 触った全モジュールの容量実測（python len）。**modUIShelf は 29,904 字以下のまま**（差し替えは同字数以内）
4. 新テストのネガティブ確認（期待値を壊して落ちる→復元）
5. 敵対的レビュー2周
6. 実機: ①📁 追加で png を選べて取り込める ②📸 取込直後に本文が出る ③❌違う→正しい内容→同じ質問で是正メモが出典の先頭に出る ④（GO 後）新版の初回起動で前版の本棚を引き継げる

## 7. リスク台帳

- 是正メモの注入が**誤爆**する（似て非なる質問に古い是正が先頭に来る）→ 完全一致を優先し、語一致は閾値を高めに。`correct_inject=off` の逃げ道。
- `text_view` を開いたまま別画面へ行くと作業シートが残る → modDiag と同じ「戻る」で削除／起動時に残骸を掃除（`HideInternalSheets` は凍結 modBoot なので、`modTextView` 自身が次回 Show の前に古いシートを消す）。
- 📁 で画像を選んだとき `IngestFile` より手前で弾かれる関所があれば E0301 のまま → 調査で確定。
- 1-A: 前版 xlsm を開くときのイベント抑止漏れ → `EnableEvents=False` を `On Error` の外で必ず戻す（Finally 型）。

## 8. 記録のみ／R37 送り

- 1-B 本格データブック分離（起動・終了の秒数を実測してから）
- 誤チャンクの減点・無効化／他人の correction の取り込み／是正メモ一覧
- 読み取り結果の編集→保存し直し
- フォルダ同期での画像取込
- `WScript.Shell`×17（実5）・`new:{`×2・`ServerXMLHTTP`×1 の撤去と `--vba-mode installer` 削除（衛生。姉妹PJの「0件照合」に追随）
- 精度: `tools/bench_retrieval.py` で数字を取ってから（凍結解除の裁定を含む）
