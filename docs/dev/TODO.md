# Nexus Agent — TODO / 既知の未対応事項

最終更新: このドキュメント作成時点(コミット `8b9d7a8` の直後)。

## A. 実機テスト前に必ず確認(最優先)

このコードベースは開発環境にExcelランタイムが無いため、静的検証
(lint/LibreOffice構文コンパイル/ビルド自己検証)は全てグリーンだが、
**実際のクリック・描画・共有フォルダI/O・複数端末P2Pは未検証**。
`docs/dev/EDGE_CASES.md`で「✅対処済み」としている項目も、実装が
正しいことの検証は静的解析(コードレビュー・アルゴリズムのオフライン
相互検証)止まりであり、実機での動作確認が必要。

- [ ] ブックを開く→マクロ有効化→初回起動が成功するか(自己インストーラの
      モジュール注入)
- [ ] オンボーディングツアーの表示/スキップ/完了、2回目起動で出ないこと
- [ ] ホットキー(Ctrl+Shift+Q / Ctrl+Enter)が他ブック作業中も含めて動くか
- [ ] P2P機能一式(2台以上での確認が必須):
  - [ ] 感謝状の送受信とEXP加算
  - [ ] Mentor質問の送受信・返信
  - [ ] ノイズ投票が閾値到達で組織的除外に収束するか
  - [ ] 管理者による除外解除が全員に反映されるか
  - [ ] チーム連帯ボードの組織合計値が正しく合算されるか
  - [ ] 共有フォルダ切断時のフェイルソフト動作(個人統計のみで描画継続するか)
- [ ] きせかえスキンの解放条件(感謝5件/20件)が正しく判定されるか
- [ ] 32bit Excel環境での動作(社内標準環境)
- [ ] 実際のDPIスケーリング環境でのレイアウト崩れ有無

## B. コードは書いたが判断待ちの機能

### B-1. 公式パック自動検出
**現状**: 未実装。設計方針のみ確定(セッション内のPM対話で合意):
`config`に`official_pack_path`のようなキーを1つ追加し、起動時にその
フォルダ内`*.mbpack`のファイル名+更新日時を取込済み記録と照合、新版が
あれば「取り込みますか?」と提案する。

**保留理由**: 「公式パックをどこに置くか」という**運用上の合意**(商品部
のシェアポ固定パス、ポータル掲載など)が組織側で未確定のため。運用の
置き場所が決まり次第、実装は小規模(新規モジュール1つ+起動フック1行)。

### B-2. 相棒の命名機能(不採用と判断した理由)
Gemini提案の「コンシェルジュに名前をつける」機能は見送った。理由:
(1) 初回起動には既にパック作成者名の入力が1つあり、2つ目のInputBoxは
導入摩擦を増やす、(2) 全通知文言の呼称差し替えは広範な変更になり
regressionリスクに見合わない。将来「設定画面」ができた際の追加候補。

## C. 既知のリスク・セキュリティ関連(コード対処ではなく運用対処が必要)

- [ ] **Azure APIキーの平文格納**: `build/build_mybookshelf.py`の
      `build_config_rows`内に`azure_embed_key`がハードコードされている
      (`embed_transport=direct`利用時のみ参照)。ブック配布=キー配布に
      等しいリスク。運用時は部署別キー発行+定期ローテーション、または
      `embed_transport=ribbon`(社内AIリボン経由)への統一を推奨。
- [ ] **共有フォルダのアクセス制御**: P2Pディレクトリ(`thanks/`/`noise/`/
      `questions/`/`board/`)へのファイル偽造は暗号学的に防がれていない
      (性善説設計)。NTFS ACLで「書込可・削除は管理者のみ」等の制限を
      検討。
- [ ] **共有フォルダのアンチウイルス除外申請**: リトライ設計により実害は
      抑えているが、当該フォルダをリアルタイムスキャン除外に登録できれば
      さらに堅牢になる。

## D. コードの技術的負債(小さいが把握しておくべき事項)

- [ ] `src/qa/modAsk.bas`は文字数上限(30,000字)に近い状態が続いている
      (現状は収まっているが余裕は少ない)。次に大きな機能を足すなら
      feedback系かhistory系をさらに別モジュールへ分離することを検討。
- [ ] `src/ui/modUI.bas`も上限近辺(モジュール分割の詳細は
      `docs/dev/ARCHITECTURE.md` §3参照。配色ロジックは`modSkin`へ
      委譲済みで一定の余裕を確保済み)。
- [ ] P2Pノイズ投票台帳(`noise/`)は確定除外後にGCされるが、確定前の
      個別投票ファイルが閾値未達のまま長期間残るケースは未検証
      (理論上は問題ないが、大量の孤立ファイルが蓄積するかは実機で
      確認が必要)。
- [ ] Vaultギャラリーで「個人ミュート(`noise:`)」と「組織的除外
      (`gexcl:`)」の見分け表示は未実装(現状は組織的除外のバッジのみ)。

## E. ドキュメント関連

- [ ] `docs/dev/MASTER_SPEC.md`(V1仕様書)は§7(モジュール契約)以降が
      実態と乖離している箇所がある。§3/§4/§6/§12は現在も正。全面改訂は
      優先度低(`docs/dev/ARCHITECTURE.md`が現行の正としての役割を担う)。
- [ ] `docs/00_はじめての方へ.md`等の非エンジニア向けガイド4本は本セッション
      では更新していない(内容が古い可能性あり。次回、実機テスト後の
      UI変更を反映して見直すことを推奨)。

## F. R33からの持ち越し

- [ ] **`feature_diffdoc`(約款差分)は同梱されているのに動かない**(R33 W4-4)。
      `optDiffDoc.bas` は配布に含まれ注入もされるが、`InvokeFeature("diffdoc",…)`
      も `FeatureEnabled("diffdoc")` も src 全体で0件＝利用者から起動する導線が
      無い。一方 config の `feature_diffdoc` は既定TRUEなので、診断画面には
      「モジュールあり/設定有効」と出て、管理者は「有効なのに使えない」と読む。
      次ラウンドで二択の裁定が要る:
      (A) 導線を1本足す(=機能追加。マイ本棚かヘルプにボタン)。
      (B) 未提供と確定させる(config 既定を FALSE にし、`build/modules.json` から
          外して注入本数とブック容量を返す。`optTts` と同じ扱い)。
      **R33波4では実装していない**。裁定「既定を FALSE にする」の唯一の変更点が
      `build/build_mybookshelf.py:651` の1行で、波4はビルドスクリプトを
      W4-8(ui_state の text_cols)以外触らない取り決めだったため、司令塔へ差し戻した。
      VBA 側(`modFeatures.FeatureEnabled`)の既定は元から FALSE で、変更は不要。
