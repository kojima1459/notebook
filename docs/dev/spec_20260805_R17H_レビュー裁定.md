# R17H: 敵対的レビュー2面(正確性A/UX・環境B)の裁定と修正仕様(2026-08-05)

基点: f3a35ed(R17波0〜3完了)。裁定者: Fable。判定基準: docs/dev/00_プロダクト憲章.md。
修正はFix波1本。凍結不変: modShelf(残65)/modBoot(残8)/modAsk(残43)/modRetrieve/modPrompts(残321)。

## FA(重大・中)

FA-1【A-H1 名寄せ辞書が永久0行】modSynonymStore.MergeAndSave の添字を LBound 起点へ
  是正(new/old両側)。マージ規則を純関数 MergeSynPairs(oldCsv, newCsv) に切り出して
  modRagParse へ置き、Store側はそれを呼ぶ。ゴールデン(新規のみ/旧のみ/上書き/先頭要素
  保持/空)で固定。書けた件数を usage_log("synonyms_built groups=N")で残す(無音失敗の根絶)。
FA-2【A-H2/B-H1 俯瞰回答の二重警告】(i) modAskGlobal に WasGlobalTurn()(TryGlobal成功で
  立て、modAskMulti.TryDecomposed 入口でリセット)。(ii) modAskRetrieve.ApplyLowHitWarning
  は WasGlobalTurn なら無操作。(iii) modUINexusDraw の信頼度バッジ描画箇所で
  WasGlobalTurn なら文言を「🔭 章の要約に基づく回答(俯瞰)」へ差し替え(同量圧縮で捻出)。
  score=0 は維持(点数は騙さない。表示だけ出自を正しく言う)。
FA-3【A-H3 ArticleEnsure がスコープ無視】ArticleEnsure に許可source集合(scopeSources
  相当)を引数追加し AppendByIds の srcBox へ。RunMultiRetrieve は scopeSources 非Nothing
  時にそれを渡す(渡せない構造なら scoped 時は呼ばない)。深掘りの広げ直し判定(n>=2)を
  注入分が満たさないよう、注入前の件数で判定するか注入をスコープ内に限定。
FA-4【A-M4 ArticleEnsure のscore水増し】挿入分の score は 0(位置は先頭のまま)。
  設計判断コメントを modAskFocus 冒頭の既存注記と整合させる。
FA-5【A-M5 同名章キー×複数資料で2件目が空】CollectChapterHits の箱詰めを src+key の
  両方一致に(chunk_id→source 対応を先に読む)。テスト追加(同名章2資料)。
FA-6【A-M7/B-H2 俯瞰が短文で不発】modRagParse.HasGlobalSignal(q) 新設(俯瞰語彙:
  全体像/全部教えて/すべて教えて/一覧/まとめて/どんなこと/どんな種類/概要 等の
  InStr判定・純関数・テスト固定)。DecomposeGate に OR で追加(off優先は不変)。
  IsTooVague は HasGlobalSignal なら False(俯瞰語の短文を聞き返しに吸わせない。
  modAskRetrieve 側1行)。docs/10 の例文と docs/45 項目33 に短文例(「全体像は?」)を追加。
FA-7【A-M8 無人同期の章要約が無制限】modOutlineBuild.SetUnattended(b) を新設し
  modShelfSync.SyncNow の入口/Finish で set/reset(同量圧縮で捻出)。unattended 中は
  章数>12 の資料の章要約をスキップして usage_log("outline_deferred")1行(手動取込/
  手動同期=非silentで全量作成される)。name_dedup は unattended でも実行可(1回のみ)。
FA-8【A-M9 検索0件で直接キーが効かない】RunMultiRetrieve の戻り前、nHits=0 かつ
  質問に条文/別表/様式ラベルあり かつ chunk_meta 非空 のとき ArticleEnsure の
  0件許容版で最大2件を seed(score=0。低関連警告が付くのは正直な表示として容認)。
  GraphActive の nHits 条件はこの経路のみ分離(単一情報源の関数に引数で意図を渡す)。
FA-9【A-M10 chunk_meta全行読みの多重】modChunkMetaStore にセッションキャッシュ
  (書込み/削除で世代リセット)。質問経路は必ずキャッシュ経由。
FA-10【B-H3 取込時間の正直さ】optOcrEta.OcrConfirmAskFor の「(完了後に検索用の準備が
  続きます)」を「(完了後に検索用の準備と章の要約が続きます。数分かかることがあります)」へ
  (ゴールデン追随)。docs/10/45 に「テキストPDFでも大きな資料は取込後に章の要約で
  数分かかる」を明記。

## FB(小+docs)

FB-1【A-L11】modAskGlobal:271 の直書き判定を共有判定(OutlineActive/GraphActive系)へ寄せる。
FB-2【A-L12】MockLLMResponse に Case "global_answer"(タグ付きダミー)を追加。
FB-3【A-L13】modSparse.TableKeys も MAX_KEYS 枠を尊重(CountSep 確認してから追記)。
FB-4【A-M6 記録+説明】synonyms が graph_outline/章キー有無に依存する事実を
  config 説明(build_config_rows)と MASTER_SPEC synonyms 節に明記。配線の分離は次期。
FB-5【B-M docs/10:88】「進捗バナーには出ません」を実態へ(手動取込中はバナーに
  「AI整理中…」「章の要約中…」が出る)。
FB-6【B-M docs/30 §12】graph_outline の呼び出し回数(章数ぶん・最大60)と off 手順を追記。
FB-7【B-M 既存本棚の案内】modIntegrity.WarnAtStartup に相乗り: my_knowledge>0 かつ
  chunk_meta=0 なら セッション1回のトースト「資料を取り込み直すと新しい構造検索
  (条文参照・俯瞰)が有効になります」(モーダル禁止・BMPのみ)。
FB-8【B-L】modAskGlobal.AbortDigest で「(要約失敗)」章は「(この章の要約は未完了です)」へ
  差し替えて表示。
FB-9 docs/45: 項目33に短文俯瞰の再現手順とバッジ「🔭」の期待、項目34に
  synonyms_built groups>0 の確認(FA-1のログ)を追記。

## 記録のみ(クローズ時にHANDOFFへ)
- A-L14 同義語追記が DistinctiveKeys 8枠を圧迫し得る(maxAdd=3・発火後の実測で再評価)。
- 名寄せの outline 非依存化(IngestFile Finish 直呼び)は modShelf 凍結解除後の次期。
- 既存本棚カードへの「再取込で新機能有効」バッジ表示は次期。

## 検証(Fix波)
lint ERROR 0(WARN既存4)→LO compile→pure 直列全PASS+dev/prodビルド確認(生成物は戻す)。
新規純関数のゴールデン。前後文字数報告。逼迫: modUINexusDraw(残194)/modShelfSync(残88)/
modRagParse(残1,582)/modGateway(残585)は同量圧縮前提で最小差分。
