# R18H: 敵対的レビュー2面(正確性A/UX・環境B)の裁定と修正仕様(2026-08-05)

基点: a41bcfa(R18波A〜C完了)。裁定者: Fable。判定基準: docs/dev/00_プロダクト憲章.md。
修正はFix波1本(FA=重大/中、FB=小+docs)。逸脱は報告制。

## FA(重大・中)

FA-1【A-H①/B-H② 2段目確認の無制限増殖+docs矛盾】
  (i) modWorkExcel にセッション記憶(mOfferAnswered/mLaunched 相当)を持たせ、2段目
  「作業用Excelを開きますか?」は1セッション1回だけ聞く(「はい」で起動済みなら以後は
  聞かず続行、「いいえ」も以後は聞かない)。OpenWorkExcelNow 経由の起動も mLaunched を
  立てる。(ii) OCR事前確認(1段目)はファイルごと維持(見積りはファイル単位が正)。
  (iii) docs/10 §5 の「手動同期はダイアログが出ない」を実装へ追随(スキャンPDFが
  あれば1件ごとに確認が出る+作業用Excelの質問は最初の1回だけ、と明記)。
FA-2【B-H1/A-L⑪ バナー高さ1行固定の回帰リスク+狭窓の重なり】modProgressBar:
  (i) 文言の推定幅が本文可視幅を超える場合、バナー高さを2行分(約46pt)へ自動拡張
  (生成時・更新時とも。縮む方向も追随)。ボタンY位置は下段基準を維持。
  (ii) ボタンShapeが存在する間は cancellable=False の更新でも MARGIN_R_BUTTONS を
  使う(本文がボタン下に潜らない)。純関数(幅→高さ判定)はテスト固定。
FA-3【A-M② 虚偽の減少警告】(i) DeleteSource と RemoveRowsByOrigin(Prefix) の直後に
  modIntegrity.RecordSaveMark 相当でマークをメモリ上の最新行数へ更新(保存はしない。
  ui_state はブックと一緒に保存/破棄されるため整合する)。(ii) SaveCheckpoint 内の
  マーク書込みを ThisWorkbook.Save の「直前」へ移す(ディスク上のマークが常に1世代
  古い問題の解消)。
FA-4【A-M③/B-M⑤ Temp誤検知+本命チェック打ち切り】modIntegrity.IsVolatilePath:
  (i) 判定対象を ThisWorkbook.Path へ(仕様原文どおり)。(ii) "\temp\" の部分一致を
  廃止し、Environ("TEMP")/Environ("TMP") の実値との前方一致+"temp1_"+".zip\" のみ。
  (iii) volatile 警告を出しても減少チェックは継続する(Exit しない)。テスト追随
  (D:\temp\正規フォルダ/\\share\Temp\ が誤検知しないこと)。
FA-5【A-M⑤ 全重複再取込で旧行全消し+done/0確定】modShelf.IngestFile:
  acceptedCount=0 のときは 7.5 の削除をスキップ(旧行温存)し、status は failed 側+
  メモ「内容が既存資料と重複しているため、既存データを保持しました」+chunk_count -1。
FA-6【A-M④ 削除失敗の残渣未回収】旧行削除が失敗(rmNum<>0 相当)したら
  resultStatus="partial"+manifest メモ「置き換えが完了していません(もう一度取り込むと
  解消します)」。E0801 ログは維持。
FA-7【A-M⑥ silent同期の毎ファイルSave】IngestFile Finish の SaveCheckpoint は
  silent 時 throttleSec:=120 を渡す(非silentは従来どおり0)。同期末尾の1回は維持。
FA-8【A-M⑦ フッターの当たり判定】フッターShapeを Fill.Visible=-1+Transparency=1
  (完全透明の塗り)にしてクリック領域を帯全体へ。

## FB(小+docs)

FB-1【A-L⑧】image_pdf_detected の detail に modUtil.SafeLeft(path, 200) を含める。
FB-2【A-L⑨】分解ゲートを Public Function DecomposeGate(modeCfg, q, minChars) として
  modAskMulti に切り出し、TryDecomposed とテストの双方が同一実装を呼ぶ。
FB-3【A-L⑩】残骸3件: modDashStat:232 コメントから「クラスタ地図」を除去/
  MASTER_SPEC の my_manifest 列定義を実装(10列・fail_count)へ更新/
  modCluster.LoadVectors の kw() 読み(消費者なし)を削除。
FB-4【B-M③】HANDOFF §8 次期課題へ3件追記: 本番BeforeClose不在(取込中×ガードは
  開発構成のみ)/my_stats キーGC/deep・quick向け軽量clarify。
FB-5【B-M④】quick/deep で modRagParse.HasCompoundSignal(q) が真のとき、回答後に
  トースト「複数の論点を含む質問は「入念に調べる」で論点ごとに分けて回答できます」を
  1回表示(実装位置は modApp.OnSend の回答後。modAsk は触らない。文言はBMPのみ)。
FB-6【B-M⑥】docs/45 項目22 に「はい」直後に新しいExcelウィンドウが手前に出るか
  (出ない端末ではタスクバー点滅を確認)の観点を追記。
FB-7【B-L⑦】vba_lint MSGBOX_REACH_ALLOWLIST へ modIntegrity.ShrinkWarnMsg /
  VolatileWarnMsg を追加。
FB-8【B-L⑧】docs/45: 項目20 に「中断直後にカードのチャンク数が0にならない」、
  項目6 に「エラー一覧に E0303 が出ない(画像PDF判定は正常動作)」、項目24 に
  「ベクトル化フェーズ中も■中断/作業用Excelが見えて押せる」を追記。

## 記録のみ(採用せず・理由つき — クローズ時に HANDOFF へ)
- B-M⑥ の AppActivate 等によるフォーカス強制: ウィンドウタイトル推測が脆く
  端末依存のため不採用。実機観測(FB-6)で問題が出たら次期。
- A-M④ の「次回同期での自動再取込」までは今回踏み込まない(partial 化+メモで
  利用者が再取込すれば解消する導線までとする)。

## 検証(Fix波)
lint ERROR 0(WARN既存4のみ)→ LO compile → pure 直列全PASS。テスト追随+新規純関数の
ゴールデン。変更モジュール前後文字数報告。modAsk/modShelfBatch(残58字相当)等の
逼迫モジュールは実測してから。ビルドはクローズ時に司令塔側。
