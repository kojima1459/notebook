# R27 実機第12報 対応仕様（2026-08-10）

基点: `71dc351`。調査3班(A=RAG根治/B=余白設計転換/C=既知バグ型横展開監査)の統合裁定。優先: 波1(RAG)→波2(余白)→波3(監査即修正)→レビュー→配布。R26はその後。

## 波1: RAG根治(②特約が検索から消える+逆質問不発)

確定機序: (1)SparseBoost無上限(KeyScore≒330×0.06=b1 20.26、cos成分2-5%=事実上キーワード検索化。曖昧判定0.6/低ヒット警告0.3/確信度55の安全装置3つが全て無力化) (2)thoroughのHyDE+会話履歴standaloneが長語キー(Len^1.5)と資料名キーを注入し105chunk資料がpool40枠を独占→src=1→逆質問はsrc>=2必須で構造的不発 (3).docの「化け判定」はWordの表セル終端Chr(7)等の構造制御文字をGarbleRatioがbadに数える誤爆で、化け扱い本文が浄化ゼロのままembed/norm_text/プロンプトへ (4)embed_stepログはn=要求数でありok数不明=観測不能。

- **F1-1 KeyScoreに上限**: modSparse.KeyScore(modSparse.bas:377-399)の戻りを config `sparse_keyscore_cap`(既定10)でmin頭打ち。SPARSE_WEIGHT 0.06×10=加点上限0.6=cosと同土俵。configはbuild_config_rows台帳へ追加。
- **F1-2 長語重みの頭打ち**: 同:389の `Len(k)^1.5` のLenを8でclamp。
- **F1-3 pool資料多様性**: modAskRetrieve.RunMultiRetrieve(:144-147)のStashDispersionPool直前に「資料ごとの最高スコア1件をpool先頭群へ引き上げる」再配列(残2,116字内・約60行なら別モジュール化せず可)。dispersionのsrc分母と最終hitsの両方に効く。
- **F1-4 GarbleRatio誤爆是正**: modExtractorPdf.GarbleRatio(:737-756)のbadからWord構造制御文字 Chr(1)(2)(5)(7)(11)(12)(14)(19)(20)(21)(30)(31) を除外(分母からも)。残411字に収まらなければ判定本体をmodExtractor(残7,699)へ移設。
- **F1-5 本文サニタイズ**: modExtractor.ExtractFileのDropGarbledPages直後に、各ページTextから c<32(9/10/13以外)を除去する関数を追加(modExtractor内)。embed/norm_text/プロンプトが同時に浄化される。
- **F1-6 .doc全滅時の代替抽出**: modExtractorWord(残2,815)に、allGarbled時に doc.Content.Text 一括読みを1回試すフォールバック。
- **F1-7 embed観測修理**: modGateway.LogEmbedStep(:172-176)のdetailへ成功数(" ok=N")追加、呼出3箇所(:325/:342/:349)にokCountを渡す(+約100字、30,000内可)。
- **F1-8 dispersion可観測性**: usage_log "dispersion" のdetailにpool内資料名top3を追加(modAskRetrieve)。
- **F1-9 逆質問の名指し誤判定緩和**: modClarify.MentionsSourceName(:606-646)規則(3)を「同じ主要語を含む資料が2件以上あるときは名指しとみなさない」へ(残2,088内)。
- 凍結(modAsk/modRetrieve/modShelf/modChunker実質/modShelfStore実質)は全て不触で成立(調査A確認済み)。
- テスト: KeyScore cap境界/Len clamp/多様性再配列(資料2種が必ずpoolに入る)/GarbleRatio(Chr(7)混じり表テキストがOK判定)を純関数テストで固定(modTestsPure24へ追加)。

## 波2: 余白の構造完治(①)

確定事実: 下の「余白」の正体は境界関所を通らず境界外へ積まれた実コンテンツ(9経路、チャットに8: 回答差し替え/信頼度バッジ/出典チップ/評価ボタン/専門家/スターター/？ガイド/ツアー…)。行Hidden はR19裁定維持で不採用(保存関所が置けず8KB→3MB膨張実測)。右はHub方式(帯右外を幅1)が実機実証済み(hub band=1010=vis-2)。

- **F2-1 関所の全数配線(最重要)**: (a)modUI.SettleChat(:767-773)に `modSkin.ExtendChatBand ws, mChatBottom` を1行(穴#2-#9一括封鎖。modUI残1,040内) (b)modHelp.ShowHelpCard末尾/modTour.DrawStep末尾/modPeekポップ末尾に同等1行 (c)RedrawChatHeaderを通る3経路(modApp.bas:638/:698、modAppState.bas:220)の直後にApplyScrollBound再適用(調査C型2-A)。
- **F2-2 列幅1の横展開**: modChrome(open・残16,926)へ SetupDashColumns/SetupShelfColumns/SetupChatColumns を新設し、dash L:BZ・本棚 O:BZ・チャット N:BZ を幅1へ(SetupHubColumnsと同型)。呼び出しは各画面の列幅リセット直後に1行(modDash残7,867/modUIShelf残527/modUI残1,040/modVaultGallery残2,130/modShared残14,594。**modHub残4字とmodKnowledge残160字は不触**)。
- **F2-3 埋め草**: 境界最終行の下端が窓高に満たない場合、最終行の高さを「窓高-下端」ぶん加算して窓下端まで塗りを届かせる(BoundAddr呼出側の共通処理としてmodViewport2へ。ホイールで転がっても白でなくbg色が続く)。RowAtFloor/RowAtの実測系はUnhide不要(Hidden不採用のため影響なし)。
- 段階2(列Hidden化)・#25/#26(旧ホームの固定境界"A1:I40")は記録のみ(次期)。

## 波3: 監査C即修正15件(実害度高×コスト小)

調査C裁定リストをそのまま実装(各項目の詳細file:lineは波プロンプトで指示):
1. modUiLock:63 拒否トーストへwaitless:=True(連打で固まる連鎖の遮断)
2. modViewport.ViewportRefitTick先頭Enter/全出口Leave(窓リサイズ起点の入れ子再描画封鎖)
3. modPack:77 Workbooks.OpenへAlertsOff/On+ダミーPassword(modExtractorExcel:181-186流用)
4. modMigrate:211 同型+ExportUserDataと対称に
5. modShelfStore.EnsureManifestSheetへNumberFormat="@"5行(先頭ハイフンのファイル名で取込死)
6. modApp.OnClearChatへmodAskの直近ターン破棄+modUIMain.mLastAnswerText=""(消したQ&Aが部内共有される穴)
7. modWorkExcel:193 失敗通知をStatusBar→ShowToast(error)へ(StatusBarは非表示設定で誰にも届かない)
8. modUIMain.OnRunDiag Fail:へLogError+失敗トースト
9. Enterを取りながらエラーハンドラの無い3本(modHelp:100/modApp:535/:551)へOn Error GoTo Done+Leave(例外で全ボタン10分死)
10. modMentor:382 共有フォルダ未設定時のLogMentorErr+文言分岐
11. modMentor:203-211 KillWithRetryを表示済み分に限定(未表示の受信質問がファイルごと消える)
12. modMigrate:265 「元のファイル」文言の誤解修正
13. modHub.OnLangCycle 成否確認後にトースト(失敗を緑で成功と言わない)
14. FollowHyperlink直前トースト4箇所へwaitless:=True
15. modDash.ShowDashboardへTrackScreen "dash"(恒久0の計測嘘)
6.のmodAsk側: 凍結のため直接触れない→modAskに既存のクリアAPI(ClearActions/NoteGeneralAnswered系)があるか実装前に確認し、無ければmodUIMain側の変数クリアのみ+mLastMode確認でEmitVerifiedQAが発火しない条件を成立させる(凍結不触の範囲で「クリア後の🟢が共有しない」を保証。困難なら司令塔へ報告)。

## 完了条件

(1)lint ERROR 0 (2)LO compile→pure全PASS(直列・並列絶対禁止) (3)--dev/--prod/--zip自己検証PASS (4)敵対的レビュー1面(KeyScore capの検索品質への副作用/SettleChat配線の境界値/15件の各対毎の回帰)→Fix→検証パス (5)pushは司令塔。検問方式厳守。実機切り分け手順(調査A-E: retrieve_mode=single比較/my_vectors目視等)は配布報告に添付。
