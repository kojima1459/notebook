# R4 追記（実装開始前に必読）

R2/R3 の実装が完了して前提が変わっている。必ず最新 HEAD を読むこと。

- `modShelf.AddFilesResult(Optional showMsgBox As Boolean = True) As String` が
  存在する。戻り値: `"ok=<n>;ng=<n>;capped=<n>;chunks=<n>;reasons=<code>:<count>,..."`。
  reasons には E0504 / image_pdf / E0302 / E0503 / failed が入りうる。
  要件E(OnAddDocs)はこれを showMsgBox:=False で呼んで文言を組み立てる。
- `modHub.bas` は容量が 30,000 字上限まで残り約328字しかない。
  R4要件F で modHub のナビ文言・遷移先を変更する際、追加コードが必要なら
  modHubStat か新モジュールへ切り出すこと(R3 が DefaultTileValue を
  modHubStat へ置いた前例に倣う)。
- `modHub.EnsureHubLayout` は描画前に `RefreshOrgTilesIfReachable` →
  `modBoard.RefreshBoardTiles` を呼ぶようになった。B(表示の共通儀式)を
  Hub に入れる際、この呼び出しを壊さない。
- テストは modTestsPure3 に追記可(現容量を確認)。
  新モジュールを作る場合は tools/run_lo_tests.py の PURE_ALLOWLIST と
  tools/vba_lint.py の CONTRACT に登録(modTestsPure3 の例)。
- 現在のテスト総数は 205。R4 完了時に 205 以上・全PASSであること。
