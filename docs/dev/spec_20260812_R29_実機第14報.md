# R29 仕様書 — 実機第14報（2026-08-12・GO受領済み）

裁定: ④文脈根治GO／層2一括（②③⑤+豊田合成）GO／削除ボタン視認性GO。①下余白と⑥本棚削除は実機切り分けによりクローズ（下記）。
調査4班（A=文脈/B=入力欄/C=配色・クリック・トースト・豊田合成/D=ScrollArea）+ユーザー実機検証の結果に基づく。

## 実機検証で確定した事実（前提の訂正・最重要）

- **「ScrollAreaはマウスホイールを止めない」は誤りだった。** ユーザー実機（Excel 365 32bit）で Hub/Dash/本棚は ScrollArea でホイールが**止まる**ことを確認。転がるのはチャット(Nexus)のみで、原因は**FreezePanes（行1-4の上部固定）併用シートではホイールがScrollAreaを素通りする**Excel挙動（画面間比較で確定）。R19時点の出所はウェブ伝聞で、仕様書上「実機実証済み」へ格上げされていた（引用ロンダリング。D班調査）。
- 帰結: Hub/Dash/本棚の下余白は**完治済み**（止まる+R28の地色）。チャットは会話画面としてスクロールが必要（ユーザー了承）+転がった先も地色。**下余白問題はクローズ**。
- ⑥本棚削除は**正常**（行クリック→🗑削除の2手順設計。実機で確認ポップアップ動作確認）。「状態」列レ点=✅取込完了アイコン（表示専用）と説明済み。
- **CLAUDE.mdとHANDOFFの該当記述を訂正すること**（波2 W2-6）。

## 容量の最新実測（R28後。逼迫は実体をmodChrome等へ）

modUIMain残129 / modUIShelf残223 / modApp残226 / modClarify残309 / modSkin残340 / modTestsPure25残365 / modUI残716 / modUINexusDraw残832 / modKnowledge残160 / modHub残50 / modAsk残1,619(凍結・WARN許容) / modChrome残9,297 / modGenPipe残13,342 / modAppState残15,085 / modAppAct残15,815 / modAskMulti残3,053 / modConvBridge残14,018 / modFollowup残13,296

## 波1(Opus): ④③文脈引き継ぎの根治

**確定した機序**: 一般モードの入念(thorough)は起草→査読→改稿の3段だが、履歴(prevU/prevA)は起草にしか渡っていない（modGenPipe.bas:385,407は履歴引数なし・:347-349に意図的設計とコメント）。査読が文脈由来の情報を「根拠不明」と指摘→改稿が削って一般論化→表示。実機usage_logの失敗4件は全て loops=2/pass=0 で機序と整合。5往復化自体は実効確認済み（配布ブックconfig=5実測・migrateのCFG_KEEP_KEYS外で旧値が残らないことも確認済み）。

1. **W1-1** `modGenPipe` に純関数 `GenHistoryBlock(prevU, prevA) As String` 新設（modAsk.HistoryBlock:723-735と同型。`;;;`区切り新しい順を「【会話n】Q:/A:」へ整形。表示順は古い→新しいへ並べ直す）。
2. **W1-2** 起草・査読(VerifyPrompt)・改稿(RevisePrompt)の**3プロンプト本文すべて**へ会話ブロックを差し込む（リボン引数prevU/prevAは現状維持=変更しない。本文埋め込みにより「リボンが;;;を往復へ割り戻すか未確認」の不確実性を無効化する狙い）。
3. **W1-3** VerifyPrompt に1文追加: 「利用者と起草者の間には上記の先行する会話があり、その内容が回答案に反映されている。会話に現れる情報を『根拠不明』『前提が示されていない』として指摘しないこと」。
4. **W1-4** RevisePrompt にも会話ブロック+同趣旨の1文（「会話に現れる情報は根拠として維持してよい」）。
5. **W1-5** `modAppState` の一般モード共通sys（:234-240）へ1文追加: 「直前の会話が示されている場合は、その続きとして答える。『これ』『その』『さっきの』等の指示語は直前の会話の内容を指すものとして解釈する」。quick/deepにも効く。
6. **W1-6** `modAppState.BridgeToastText`（:133-141）の「直前の1往復」誤表記を件数を言わない形へ（例:「直前の会話を引き継ぎました。新しく始めるなら🗑クリア」「直前の会話を保存しました。続きとして聞くには🔍深掘り」）。シグネチャ不変。
7. **W1-7 テスト**: GenHistoryBlock の純関数テスト（1往復/5往復/空/;;;混入回答/順序）を modTestsPure28 へ。ネガティブ確認必須。設計コメント(:347-349)を裁定変更として書き換え。
8. **記録のみ**: modAsk.HISTORY_MAX_TURNS=3のconfig化は次期（凍結+WARN域のため）。RAG入念(modAskMulti.TryDecomposed)内の履歴扱いは未調査=次期監査候補。

**繋ぎ目**: CallLLM呼び出しのシグネチャは変えない（プロンプト文字列の中身だけ変わる）。プロンプト肥大はユーザー了承済み。会話ブロックは5往復×最大約5,000字/往復のためプロンプト組み立てのString連結が1023字/行制約に触れないよう既存の`&`連結断片方式を維持。

## 波2(Sonnet): 層2一括+視認性+文書訂正

1. **W2-1 入力欄の案内訂正**（B班確定: 入力欄は画面最上部のFreezePanes固定領域=行3。「下の入力欄」は実装と矛盾する誤文言）: `modClarify.bas:131,134` / `modAskMulti.bas:403` / `modAppAct.bas:151` の「下の入力欄」→「**画面上部の入力欄（いちばん上の白い横長の枠）**」へ。modClarify残309字のため文言は簡潔に（増分を実測して報告）。
2. **W2-2 逆質問中のヒント表示**: 入力欄直下のヒント行（modUINexusDraw.bas:539-561 DrawInputHint系のC4セル文字列）に clarify-pending 分岐を追加: pending中は「👉 ここ（上の白い枠）に番号を入れて送信」。実体（分岐文字列の決定）は modChrome か modAppState へ置き、modUINexusDraw への追記は最小。逆質問消滅（回答成立/クリア/TTL失効）後の次描画で通常ヒントに戻ること（状態はmodClarify.HasPending()を毎描画参照＝新規状態変数を作らない）。
3. **W2-3 トースト表示時間の可変化**: `modChrome` に純関数 `ToastWaitMsFor(msg) As Long`（ToastHeightForと同じ幅推定を流用: 全角15字/秒相当＝読了時間、**最低3,000ms・上限9,000ms**）。`modSkin.ShowToast` の `ToastWait 1100`（:413）を `ToastWait modChrome.ToastWaitMsFor(message)` へ置換（modSkin残340字内・実測報告）。waitless経路は不変。連続トースト（待機中の再入で同名Shape掛け替え）の挙動をレビュー観点として明記。
4. **W2-4 着せ替え巡回順**: `modSkin.CycleSkin`（:317）の `orderList` を `Array("msad","dark","light","sakura","ocean","gold")` へ（1回目で必ず視覚差が出る）。labels配列も同期して並び替え。テーマ解放ゲート（ThanksCount）との整合を確認。
5. **W2-5 多様性介入のスコア下限**: `modSparse.DiversitySwapPick`（:566）に相対下限を追加: **pool内別資料代表のスコアが、現hits最下位スコアの70%未満なら差し替えない**。config `diversify_min_ratio_x100`（既定70・0で旧挙動=無効化ではなく無条件差し替え）を build_mybookshelf.py に追加。純関数テスト（69%で不発火/70%で発火/0で旧挙動）を modTestsPure28 へ。
6. **W2-6 文書訂正（前提の恒久修正）**: CLAUDE.md の VBA落とし穴「`ScrollArea`はマウスホイールを止めない」を「`ScrollArea`はホイールも止める（R29実機実証）。ただし**FreezePanes併用シートではホイールが素通りする**」へ訂正。`modViewport.bas:8-22` の設計コメントも同趣旨に訂正（コード挙動は変えない）。
7. **W2-7 削除ボタンの視認性**: 本棚一覧ツールバーの🗑削除ボタンを取込系と分離（右端配置）+赤系配色（背景 #DC2626 系×白文字でコントラスト4.5:1以上を検算）。ツールバー描画の実体がどこか（modKnowledge=残160字の場合は**1文字も足せない**ため、実体を modChrome へ移すか配列定義の変更だけで済む形を設計）を着手前に実測し、入らなければ設計案を司令塔へ報告してから進める。ボタン数・並び順の変更に伴う OnAction 配線の全数確認。
8. **テスト**: W2-3/W2-5 の純関数テスト+ネガティブ確認。既存2,286件全PASS維持。

## クローズ・記録のみ

- ①下余白: クローズ（Hub/Dash/本棚=完治実証、チャット=FreezePanes起因の仕様限界+会話画面として自然+地色継続）。
- ⑥本棚削除: 正常動作確認でクローズ。「状態」列は✅⏳⚠️🖼の状態アイコン（表示専用）と説明済み。
- 記録のみ（次期）: チャットのFreezePanes×ScrollArea素通りの深掘り（分割ウィンドウ設定等の回避策があるか）／ヘッダー見出し「状態」への凡例表示／HISTORY_MAX_TURNS config化／RAG入念内の履歴監査。

## 共通規律

検問方式（着手空コミット・項目毎コミット・pushは司令塔）／凍結5本不触／dist非コミット／DoD全6条件（lint ERROR 0・LO compile→pure直列全PASS・dev/prodビルド・容量実測・ネガティブ確認・残課題明示）／完了後に敵対的レビュー→Fix→Fix検証パス→final-gates。
