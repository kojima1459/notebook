# R12: 未踏観点監査の統合裁定と実施計画(Fable司令塔 2026-08-01)

対象HEAD: a541474。入力: (1)3観点監査(移行互換/異常入力/スケール。報告全文は
セッション記録)、(2)8観点並列監査のうち完了4本(ライフサイクル/ロケール/
セキュリティ/RAG。全文= audit_20260801_R12_未踏観点監査_生報告.md)。
**8観点中4本(テスト網羅/ビルド完全性/アクセシビリティ/リソース)は
API月次上限超過で未実行。上限回復後に最優先で実行すること。**

## 0. 状況

- 監査中にAPI月次上限へ到達。実装エージェントの起動不可。
- 本書は「発見の永続化+裁定+実施計画」であり、実装は上限回復後。
- 配布済みzip(a541474)への影響: 下記R12-1の各件は配布物にも存在するが、
  いずれも通常操作では発現しない(発現条件を§4に明記)。実機スモークは
  続行可。

## 1. 裁定済みの修正計画(上限回復後に波で実施)

### R12-1【最優先・小粒の重大件】
1. **modGuard.bas:183 の knowledge_expire_days フォールバックを 30→0**
   (High。configセルが空/文字列化した瞬間に30日消去タイマーが起動する
   逆向き既定。1行+読めなかった事実の usage_log。modBoot:391 のコメントも)。
2. **modApp.OnSaveAndExit: ThisWorkbook.Close の直前に modBoot.Auto_Close
   を明示呼び出し**(Critical。本番ビルドのThisWorkbookはインストーラ占有で
   BeforeCloseが無く、VBAからのCloseはAuto_Closeを実行しない→終了ボタン
   経由だと OnKey残存(Excel全体でCtrl+Z死亡)/リボン非表示残存/テレメトリ
   未送信。Auto_Closeは冪等)。
3. **streak(連続利用日数)が実機で恒久1**(Med・無条件発現): modStats.
   TouchToday の比較前に IsDate 正規化(ShortDateと同型)。書き側の
   NumberFormat="@"化は移行考慮が要るため比較側で吸収。
4. **和暦カレンダー設定への防御**: guard_last_reach 等の日付書込を
   Format$("yyyy-mm-dd")から Year()/Month()/Day() 数値合成へ(和暦設定で
   元号年が書かれ失効判定が誤作動/無言無効化する芽)。同型の日付文字列
   書込を grep で全数確認し同時に統一。
5. modEmbed.SleepMs に Timer日跨ぎガード1行(同型9箇所で唯一の欠落)。
6. modPrompts.bas:361 BuildSourceBlock の生Left$→modUtil.SafeLeft
   (R11-I適用漏れ。サロゲート境界)。
7. ValidateMigFile に mig_format_version 上限チェック1行(前方互換)。
8. shelf_max_chunks フォールバック不一致(20500/20000/10000)を定数1箇所へ。

### R12-2【セキュリティ/プライバシー】
1. **SanitizeForCell を modUtil へ共通化**し、未信頼テキストのセル書込
   3経路(modInsightIo.AppendRow / modPack.ImportChunksDedup /
   modStats.SetStatValue(version.txt値))へ適用(数式注入の整合性欠陥)。
2. **EmitGap のプライバシー**: 検索0件時に質問全文+実名+部署が無設定で
   共有フォルダへ自動送信され、docs/30 に開示が無い。
   裁定案: config ゲート(insight_share_enabled 既定TRUE=機能価値は維持)+
   docs/30 への明確な開示+送信内容の見直し(全文か先頭Nか)。
   **既定値と送信範囲は利用者(プロダクトオーナー)の決定事項として提示する
   こと(私見: 既定TRUE+全文維持+開示追加。理由: 共有知が本製品の核で、
   社内共有フォルダという閉域への送信であるため)。**

### R12-3【堅牢化Med】
1. modMigrate.RestoreSheet の無言失敗解消(Boolean化+E0801+復元行数実測
   ログ+失敗時ダイアログ文言切替)。
2. MergeConfig のホワイトリスト化(利用者設定キーのみ引き継ぐ。既定値
   変更の巻き戻し防止)。
3. 恒久失敗ファイルのバックオフ(manifest に連続失敗回数、3回で
   failed_permanent。明示再試行で復帰)。
4. txt/md/csv のサイズガード(FileLen 50MB超は明示失敗+日本語案内)。
5. insight_inbox: consumed済み行のトリム(modChatLogの100件ローテと同作法)
   + SameQuestionCount の一括Range読み+Dictionary化(O(n²)→O(n))。
6. **出典ページずれ(RAG監査1)**: GsPageBounds の先頭スキップが物理ページ
   番号を1つ以上ずらす(画像表紙+テキスト本文のPDFで現実的)。ページ番号を
   物理添字基準へ。純テスト更新。
7. binary_rag の粗選別が「条番号完全一致の決定的救済」を先に落とす件:
   DistinctiveKeys 完全一致行を候補集合へ強制union(既定FALSEのため
   発現は限定的だが設計保証との矛盾)。
8. lifecycle Med: X閉じキャンセル後のホットキー/自動同期の自己修復
   (EnsureAppView相当へ再登録を追加) / OnTime・OnKey の Procedure を
   'ブック名'! 修飾+Auto_Close の解除を自ブック設定時に限定(新旧併存)。

### R12-4【スケール設計(要件定義から)】
検索が公称上限20,500チャンクで分オーダー化する件(支配項はベクトルCSVの
毎回パース+テキスト正規化のやり直し)。対策候補: セッション内ベクトル
キャッシュ(modBitwiseOpt.BuildCacheと同型)/正規化済み列の取込時前計算/
大規模時 binary_rag 既定化(現状、出荷構成で一度も作動しない)/行ループへ
DoEvents+進捗。**docs/70 の公称値(20,500件≒150〜200冊)を実測に合わせて
改訂することも含め、独立した要件定義を書いて実施(場当たり最適化禁止)。**
併せて: modBitwiseOpt キャッシュ鍵に再埋め込み世代を混ぜる(RAG監査3)。

### R12-5【小粒Low】
インストーラ保存失敗の観測(Save後Err確認+注入失敗のf計上)/
ScheduleAutoSync 失敗を LogUsage へ/Auto_Close の ActiveWindow 復元に
自ブックガード/L-23フォールバックの時刻精度(数値セル直書き化)/
FindKeyRow 裁定前提の更新(nonce行成長。必要なら別シート分離)/
insight_inbox の未取込行を引き継ぎ対象へ(または ins: 既読印を運ばない)。

### R12-6【未実行監査の完了】
テスト網羅メタ/ビルド完全性/アクセシビリティ/リソース長期耐性の4観点を
上限回復後に実行し、本書へ追記裁定する。

## 2. 見送り・受容(理由)

- FindKeyRow の即時最適化: 見送り継続(まず前提行数の事実更新。体感数百ms)。
- 検索スケールの応急パッチ: 禁止(R12-4で設計から。PoC規模では未発現)。
- 数式注入のRCE懸念: 管理端末のDDE既定無効によりLow(セル破損・同期ループ
  の実害ベースでR12-2-1を実施)。

## 3. 検収基準

各波: lint ERROR 0 / LOテスト全PASS(496+新規) / 両ビルド自己検証OK。
R12-1完了時点で配布zip再生成・再配布。R12全完了後に敵対的レビュー。

## 4. 配布済みzip(a541474)利用時の注意(実機テスト継続のため)

- config の knowledge_expire_days セル(値0)を**空にしたり文字を入れたり
  しない**こと(空にすると30日タイマーが起動する既知欠陥。R12-1-1で修正)。
- アプリ内「終了」ボタンで閉じた後、Excel全体の表示(リボン等)や Ctrl+Z が
  おかしい場合は **Excelを完全終了して開き直す**(R12-1-2で修正)。
  スモーク手順(12)の終了確認はこの注意付きで実施可。
- 連続利用日数タイル/7日連続バッジは常に1日表示になる既知欠陥(R12-1-3)。
  スモーク判定の対象外とすること。

## 5. 監査8観点の完了と追加裁定 2026-08-01(R12-1完了後)

R12-1 は 78a33e6 で完了(8/8)。残り4監査(テスト網羅/ビルド/a11y/リソース)
完了。生報告は audit_20260801 に追記済み(見出し6-8の入替を修正済み)。
追加裁定:
- **R12-3へ追加**: modChunker 構造モードの文字列連結O(n²)を配列+Joinへ
  (見出し無しMB級txtでの分単位ハング。JoinSplitNumbersも同時に)/
  IsoDateCompact(yyyymmdd系統計キーの元号防御)/modChannel:659コメント修正。
- **R12-7【a11y】新設**: 信頼度バッジのテーマ別tint(dark/goldで比1.0台=
  安全文言が消える・High)/利用者バブル下端の文字コントラスト/
  Hubアイコンラベル6pt→8.5pt+/押下チップ7.5pt→8.5pt+/評価ピルの
  濃色化+9.5pt/muted色の4.5:1未満テーマ調整。
- **R12-8【テスト補強】新設**: make_seed_pack.py のFNVハッシュを
  VBA(UTF-16コードユニット+改行保持)と互換に修正+両側ゴールデン値
  テスト(High。現状シード重複排除が全文字列で不一致)/未テスト高リスク
  純関数へのテスト追加(modRagParse4本全滅・modBitwiseOptビット境界・
  modFollowup3本・TruncateAndRenorm・HasVector・ElapsedMsSince・
  BlendPerItemMs・MergeAnswer・BuildRerankPrompt↔ParseRankOrder対)。
- **R12-9【ビルド堅牢化】新設**: --dev --zip の同名上書き禁止(dev名分離)/
  Stage6失敗時の原子的確定(一時パス→os.replace・不良品退避)/
  modules.json name/path一意性検査/zipのGS実体検査/配布zipへ docs/45+
  README同梱(憲章§5-3適合)/pad_to_exact のBuildError化。
- 記録(次期): パック書き出しのベクトル複製ピーク(上限時~1GB。バッチ化の
  方向を記録)/build_stamp dirty表記ほか各Low。
実施順(排他): R12-3(Opus)→R12-2+R12-7(Sonnet)→R12-8(Sonnet)→
R12-9+R12-5(Sonnet)→敵対的レビュー→配布zip。

## 6. R12敵対的レビューの裁定 2026-08-01(判定: 差し戻し→R12-Hで対応)

High-1: R12-3の救済union(既定OFF前提の全行走査)がR12-4のbinary_rag自動化で
既定経路化しスケール目標を毀損=波間相互作用の退行。→前計算norm_text照合+
vbBinaryCompare+Tick化。Med-4(生テキスト照合の字詰め取りこぼし)も同修正で解消。
High-2: 検索中DoEventsへAutoSyncTickが割込みResetVecCacheで残行無言スキップ。
→SyncNowの関所連携+Ready再確認+ログ+やり直し。
Med-3: modBoard読み側2箇所のIsoDateCompact取りこぼし(和暦回帰)。
Med-5: 移行経路の数式注入再開口(SanitizeForCell適用)。
Med-6: バックフィル200行バッチ化+失敗ログ。Med-7: 世代加算4箇所補完。
Low-10/11/12/14: gitignore残骸/トリムカウンタ分離/lint契約Pure8/err#0ノイズ。
記録(次期): Low-8(kData10列読みのピーク見積り。full_text遅延取得化)/
Low-9(VBAリセット後のOnTime二重予約の残余=予約時刻リスト化)/
Low-13(ブック名アポストロフィの修飾二重化)。
レビューが「問題なし」を全数確認した範囲: DotAt数値等価/FNVゴールデン値の
標準ベクトル照合(自己整合の罠なし)/10列化の全読み書き整合/norm_text式の
一本化/MergeConfigホワイトリスト全数/ビルド原子性/zip4名分岐ほか。
