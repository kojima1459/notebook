# R20H レビュー裁定（2026-08-06）

基点: `f4f3cbb`（R20波A〜D完了）。敵対的レビュー2面（面1=正確性/Opus、面2=回帰/Sonnet）とおもてなし監査の指摘を裁定し、Fix波の作業指示とする。凍結5モジュール不触・ビルド安全性（BeforeClose無し/ストリーム上限52B余裕）・波B/Dの主要ロジックは両面とも適合確認済み。

## FA: 必須修正（Fix波）

- **FA-1 [BLOCKER] ビルド台帳の抜け**: `build/modules.json` に modBackfill(src/ingest, ingest)・modSetupWizard(src/ui, ui)・modTestsPure20(src/test, test) の3件を追加。既存エントリの書式に厳密に合わせる。追加後 `python3 build/build_mybookshelf.py --dev` が check_unregistered を通過することを確認。
- **FA-2 [BLOCKER] 再フィットのbusy判定不足**: modViewport.bas:470-471 の isBusy を `modUiLock.IsBusy() Or modShelf.IsBusy() Or modShelfSync.IsBusy()` へ（R15-FixA FA-3iii と同型の是正）。純関数 RefitAction は無変更。取込・同期・仕上げ中の DoEvents で再フィットが走らないことをテストで固定。
- **FA-3 [MAJOR] ⚡仕上げのUIロック欠落**: modKnowledgeBar.OnBackfillClick を `If Not modUiLock.Enter() Then Exit Sub` … `modUiLock.Leave` で囲む（modHelp の全ハンドラと同型）。二重実行・入れ子取込を遮断。
- **FA-4 [MAJOR] 本棚行高高水位のモード間共有回帰**: mShelfRowHigh（行高リセット用）と「一覧本文をどこまで書いたか」を分離。modUIShelf.ClearCardArea は table 専用の前回本文行（新設のモジュール変数）を使い、`Max(自モード前回行, ShelfRowHigh())` を下限とする。再現: table60冊→gallery→table絞り込みで旧行残存、をテスト固定。
- **FA-5 [MAJOR] ☀🌙トグルの死亡**: modUI.ToggleTheme の「特別スキン」分岐を sakura/ocean/gold のみに限定し、msad はライト側として扱う（msad/light ⇄ dark。dark からの復帰先は msad）。modUI 残約1,878字のため等量圧縮を意識。
- **FA-6 [MAJOR] 部門ラベルの空約束**: 配線は次期送りとし、modChannel.ActiveLabelText の未設定文言を「📚 部門資料: 未設定（❓ヘルプの⚙から設定）」へ（実行可能な案内に修正。「クリックで設定」を出さない）。
- **FA-7 [MAJOR] ダッシュ管理者行の版面割れ**: modDash.bas:420/454/475/489 の `modDashStat.KPI_X0` → `modDashStat.RowX0()`（サブタイトル:341は左寄せ意図で据え置き、コメント明記）。
- **FA-8 [MINOR] フッター段数のモード間リーク**: modLive.Footer 側で mode="normal" のときは段数を一切参照・表示しない分岐（層が閉じる案を採用）。
- **FA-9 [MINOR] 初回ウィザードの再出現**: modSetupWizard に専用キー `nexus_wizard_done` を新設し、RunFirstRunWizard 冒頭チェック＋末尾で必ず "1" 保存（スキップでも保存。再入口は Hub カード）。ツアーのフラグとは独立。
- **FA-10 [MINOR] 自己エコー防御の全角スペース抜け**: modClarify.IsSelfEcho で ChrW(&H3000)→半角へ正規化してから Trim。あわせて modTestsPure18 の「8字未満の別文言」テストを実装と整合する名前・内容へ是正し、全角スペース境界テストを追加。
- **FA-11 [MINOR] 部門ピッカーの無言失敗**: modSetupWizard.PromptDept で数字以外の入力時に `modSkin.ShowToast`（「1・2・0のいずれかで入力してください。Hubの案内カードからやり直せます」系）を出す。
- **FA-12 アプリ名の二重人格**: modAppDef.APP_NAME "Nexus Agent"→"MyBookshelf"。**変更前に全参照を走査し、表示（MsgBoxタイトル・ラベル）以外の用途（パス・キー・識別子・OnTime修飾等）が1件でもあれば変更せず報告**。表示専用と確認できた場合のみ変更し、目視相当の確認として代表箇所のgrepリストを報告に添付。
- **FA-13 backfill判定の大小比較統一**: modBackfill の CollectSourceChunks（vbTextCompare）と ScanKnowledge（binary Dictionary）の比較方式を片方に統一（推奨: 両方 vbTextCompare 相当）。
- **FA-14 graph_outline=off の未仕上げ誤表示**（波C裁定済み）: DetectLegacyDocs で `graph_outline=off` のときは doc_outline 欠落を「未仕上げ」根拠にしない。
- **FA-15 docs是正バンドル**（おもてなし高優先）: docs/10 = 質問テンプレチップ記述削除・§3ダッシュボードをKPI4枚の実物へ全面差し替え（レベル/EXPの一言説明付き）・バッジ一覧を13種へ・§4表の空行破損修正・「2速」→「回答モード（3種類）」改題・「初回セットアップ」節新設・「引き継ぎファイル」を独立節へ昇格。docs/00 = ステップ3.5（初回ウィザード最大4連続ポップアップ・全てスキップ可）を追記。
- **FA-16 生ジャーゴン文言バンドル**（おもてなし中優先の裁定採用分）: 本棚列見出し「チャンク数」→「分割数」（modUIShelf:197）／ダッシュKPI「蔵書チャンク数」→「資料の分量」（modDashStat:354。docs/45の該当記述も追随）／modHelp の「P2P共有フォルダ」「P2P接続設定」→「共有フォルダ」系へ・例示パス Nexus_Share→MyBookshelf_Share／modBoard:182-184 の「P2P機構」文言を平易化／modKnowledge:632-636 の内部パス構造案内を管理者向けへ簡略化／modVault:217 の「ベクトル化されました」→「AIが読み込みました」＋タイトルをAPP_NAME参照へ／modHelp:514-516 かんたんガイドの画面説明を実タブ構成に一致させる。

## FB: 検証・完了条件（Fix波）

1. vba_lint ERROR 0（直列）
2. run_lo_tests --mode compile OK → --mode pure 全走 PASS（直列）
3. `build_mybookshelf.py --dev` と `--prod` の自己検証通過（FA-1の確認を兼ねる）
4. FA-2/FA-4/FA-9/FA-10 はテストで固定（形だけ禁止）
5. コミットは意味単位、push は司令塔

## 記録のみ（次期・実装しない）

- 部門ラベルのクリック配線（modUINexusDraw のピル化。残2,013字のため分割裁定とセット）
- Hub左カラム（統計タイル/バッジ帯）の幅追随
- ACCENT #07A963 の白字コントラスト未達箇所（既存継承・modSkin管掌外の各所）
- darkテーマのヘッダー帯がブランド色固定（ApplyHeaderDepth のテーマ非依存・既存継承）
- modKnowledgeBar.ToolbarContentRight が呼び出し元ゼロ（孤児化・契約掃除）
- DetectLegacyDocs の breadcrumb 診断を先頭1チャンク→複数チャンク多数決へ
- MaybeShowBackfillToast が本棚初回描画中に全走査する重さ（2万行級で体感遅延の可能性）
- OnTime 予約時刻の ui_state 二重化（modShelfSync 同型の恒久化）と mRefitRunning の失効ガード
- ブック名に「'」を含む場合の OnTime 修飾（既存作法全体の問題。直すなら一括）
- 「ナレッジ」⇔「資料」の全面用語統一（影響広のため次期一括）
- docs/10 目次の全面再構成（今回は FA-15 の部分再編まで）
- 分割裁定が必要な逼迫モジュール: modSkin(残1,310)/modHub(1,424)/modKnowledge(1,519)/modHubStat(1,569)/modUIMain(1,642)/modChannel(1,694)/modUI(1,878)/modTestsPure2(458)
